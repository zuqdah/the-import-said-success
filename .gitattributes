# Anything a Linux runner executes or parses keeps LF endings. A .sh checked out
# with CRLF fails with a bad interpreter error naming a path that looks correct.
*.sh text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.tf text eol=lf
